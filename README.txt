BM Trading — Terminal Emerald Fixed

Fix pass:
- FAQ sidebar no longer uses sticky positioning on mobile/tablet.
- FAQ accordion defaults closed on mobile so it cannot overlap the FAQ intro card.
- FAQ accordion now behaves as a single-open accordion.
- Mobile FAQ spacing, tap target sizing, and answer text spacing improved.
- Added a small fail-safe so the page does not remain hidden if JS loads slowly.

Deploy on Vercel as a static site. No build command required.
